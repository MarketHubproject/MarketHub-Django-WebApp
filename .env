# Local Production Testing Environment
# This uses SQLite instead of PostgreSQL for easier local testing

# Django Core Settings
SECRET_KEY=django-test-prod-key-change-for-real-production-12345
DEBUG=False
DJANGO_SETTINGS_MODULE=markethub.settings.prod

# Allowed Hosts (for local testing)
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (SQLite for local testing)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db_prod_test.sqlite3
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_HOST=localhost
DATABASE_PORT=5432

# Email Configuration (console for local testing)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=test@example.com
EMAIL_HOST_PASSWORD=test-password
DEFAULT_FROM_EMAIL=noreply@localhost

# Redis Cache Configuration (use local memory for testing)
REDIS_URL=

# Stripe Payment Configuration (test keys)
PAYMENT_ENV=test
STRIPE_PUBLISHABLE_KEY_LIVE=pk_test_51234567890abcdef
STRIPE_SECRET_KEY_LIVE=sk_test_51234567890abcdef
STRIPE_WEBHOOK_SECRET_LIVE=whsec_test_51234567890abcdef

# CSRF Protection
CSRF_TRUSTED_ORIGINS=https://localhost,http://127.0.0.1:8000

# API Configuration
API_BASE_URL=http://127.0.0.1:8000/api/

# Performance & Monitoring
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1
ENVIRONMENT=production-test

# Static Files
STATIC_HOST=
MEDIA_HOST=

# API Rate Limiting
API_THROTTLE_ANON=100/hour
API_THROTTLE_USER=1000/hour